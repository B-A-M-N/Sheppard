---
phase: 12-06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/e2e_verifier.py (new)
  - scripts/benchmark_suite.py (add verification section post-benchmark)
  - .planning/phases/12-06/e2e_report.json (output)
  - .planning/phases/12-06/e2e_verification.md (output)
autonomous: false
requirements:
  - E2E-01
  - E2E-02
  - E2E-03
must_haves:
  truths:
    - "No app code changes — verification only"
    - "Full pipeline executes without exceptions"
    - "All section citations match DB atom_ids_used"
    - "Validator re-check passes on stored artifacts"
    - "Mission timeline reconstructable from logs/metrics.jsonl"
    - "Guardrail tests 99/99 before and after"
    - "No truth-contract weakening or validator bypass"
  artifacts:
    - path: "scripts/e2e_verifier.py"
      provides: "Verification runner: DB queries, span log validator, provenance checker, validator re-run"
      contains: "verify_mission_integrity|verify_synthesis_citations|validator_recheck"
    - path: ".planning/phases/12-06/e2e_verification.md"
      provides: "Human-readable report of E2E execution results and pass/fail status"
      contains: "E2E-01:|E2E-02:|E2E-03:"
  key_links:
    - from: "e2e_verifier.py"
      to: "benchmark_suite.py"
      via: "runs after benchmark, reads same mission data"
      pattern: "mission_id"
---
<objective>
Phase 12-06: High-Evidence E2E Integration Test — Run benchmark suite with high_evidence scenario, then execute verification checks against DB, logs, and artifacts to prove the full truth chain works correctly. Produce a machine-readable report (e2e_report.json) and human-readable verification (e2e_verification.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-06/12-06-CONTEXT.md
</context>

<tasks>

<task type="interactive">
  <name>Task 1: Create e2e_verifier.py — End-to-end verification runner</name>
  <read_first>
    - .planning/REQUIREMENTS.md (E2E-01, E2E-02, E2E-03)
    - scripts/benchmark_suite.py (high_evidence benchmark runner)
    - src/research/reasoning/synthesis_service.py (validator logic)
    - src/utils/structured_logger.py (span logs)
  </read_first>
  <acceptance_criteria>
    - scripts/e2e_verifier.py exists and is executable (python3 -m py_compile exits 0)
    - Contains verify_mission_integrity(mission_id) that checks: atoms retrieved ≥1 per section, stored sections have valid artifacts
    - Contains verify_synthesis_citations(mission_id) that checks: all citation FKs match atom_ids_used in DB
    - Contains validator_recheck(mission_id) that re-runs validation on stored content
    - Contains check_span_log(mission_id) that verifies logs/metrics.jsonl contains span_start+span_end for all 6 stages
    - Contains check_contradiction_v3(mission_id) that verifies no legacy memory.get_unresolved_contradictions calls
    - Run python3 -m py_compile scripts/e2e_verifier.py exits 0
  </acceptance_criteria>
  <action>
    1. Create scripts/e2e_verifier.py with async functions:
       - verify_mission_integrity(mission_id) — queries DB for section count, atom presence
       - verify_synthesis_citations(mission_id) — checks FK consistency between synthesis_sections.atom_ids_used and synthesis_citations
       - validator_recheck(article_text, atom_map) — re-runs the validator logic on stored report text
       - check_span_log(mission_id) — parses logs/metrics.jsonl for complete spans
       - check_contradiction_v3() — greps codebase to confirm no legacy contradiction calls
       - check_no_app_code_changes() — git diff check on src/core and src/research
    2. Each function returns (pass_fail: bool, details: str)
    3. Main function aggregates results into e2e_report.json
  </action>
</task>

<task type="auto">
  <name>Task 2: Run E2E benchmark and verify</name>
  <read_first>
    - scripts/benchmark_suite.py
    - scripts/e2e_verifier.py (from Task 1)
  </read_first>
  <acceptance_criteria>
    - Command `PYTHONPATH=$PWD:$PWD/src python3 scripts/benchmark_suite.py --scenario high_evidence --iterations 2 --output phase12-06_benchmark.json` exits with code 0
    - Command `PYTHONPATH=$PWD:$PWD/src python3 scripts/e2e_verifier.py --mission-id <from_benchmark> --output .planning/phases/12-06/` exits with code 0
    - Guardrail tests pass (99/99)
  </acceptance_criteria>
  <action>
    1. Run benchmark with 2 iterations: `PYTHONPATH=$PWD:$PWD/src python3 scripts/benchmark_suite.py --scenario high_evidence --iterations 2 --output phase12-06_benchmark.json`
    2. Extract mission_id from output JSON
    3. Run verifier: `PYTHONPATH=$PWD:$PWD/src python3 scripts/e2e_verifier.py --mission-id <id> --output .planning/phases/12-06/`
    4. Check e2e_report.json — all checks must PASS
    5. If any fail, analyze and report which assertions failed
  </action>
</task>

</tasks>

<verification>
- E2E-01: Full pipeline executes without exceptions (benchmark exit code 0)
- E2E-02: Verification assertions pass:
  - At least one atom per section average
  - All citation FKs match DB
  - Validator re-check passes
  - atom_ids_used matches synthesis_citations
- E2E-03: Deterministic topic (high_evidence), reproducible
- Guardrails: 99/99 tests pass
- No truth contract weakening (no app code changes)
- No validator bypass
- No mission_id/provenance changes
- No ranking changes
</verification>

<must_haves>
- E2E-01: Full pipeline executes end-to-end
- E2E-02: All verification assertions pass
- E2E-03: Deterministic topic, reproducible
- No app code modifications
- Guardrail tests 99/99
</must_haves>

<success_criteria>
- e2e_verifier.py created and runs successfully
- E2E benchmark completes 2 iterations
- All verification checks PASS in e2e_report.json
- e2e_verification.md documents results
- Ready for 12-07 ranking optimization
- Explicit note on 12-03 single-endpoint bottleneck impact on E2E throughput
</success_criteria>

<output>
After completion, create:
- .planning/phases/12-06/e2e_report.json (automated output from verifier)
- .planning/phases/12-06/e2e_verification.md (human-readable summary)
- .planning/phases/12-06/12-06-SUMMARY.md (phase completion notes)
</output>